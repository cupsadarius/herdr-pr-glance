# Checkpoint after Task 4

Paused on 2026-09-08 at the user's request, after completing Task 4 and its review
gates. Resume with Task 5 only when the user asks to continue.

Branch: `feat/glance-pr`. Implementation head: `202281c`.
Nothing has been pushed or published.

## Completed

| Task | Result | Final implementation commit |
| --- | --- | --- |
| 1 | Working-pane/source resolution and bounded command runner | `2ac2867` |
| 2 | Exact PR statistics, paginated CI, gh discovery/error handling | `01c15b9` |
| 3 | Complete discussion pagination and private atomic disk cache | `129446d` |
| 4 | Bubble Tea state, refresh/cache policy, cancellation and cooldown | `202281c` |

Each task passed separate spec and quality gates. Fixes include branch/tag name
ambiguity, fork/upstream no-PR responses, unrelated cache cleanup failures, obsolete
discussion results overwriting newer cache, and manual refresh lost during a cache
read. The parent inspected the UI transitions and persistence fix and supplied the
manual-refresh regression finding. The full-application final review is still pending.

## Validation

- macOS ARM64, Go 1.27.1: all package tests and vet passed; Task 4 UI race checks
  passed on the final implementation. Tracked Go files are gofmt-clean.
- Linux ARM64 under OrbStack, Go 1.25.0: `go mod verify`, `go vet ./...`,
  `go test ./...`, and `go test -race ./...` passed at `202281c`, using a disposable
  non-root container and a copied checkout.
- Read-only live GitHub checks used public `herdrdev/herdr#3721`: snapshot,
  10 conversation comments and 4 submitted reviews. Nested inline-thread
  pagination is covered synthetically; that public fixture had no inline threads.
- An isolated Herdr 0.8.2 server and real PTY client were prepared successfully,
  then shut down at this checkpoint. Plugin pane integration has not been tested.

The Go module requires 1.25.0 and pins Bubble Tea v2.0.9. No executable entrypoint,
usable rendered UI, plugin manifest, installer, or release artifacts exist yet.

## Next work

Continue the approved [implementation plan](../plans/2026-09-08-glance-pr.md):

1. Task 5: rendering, keyboard/mouse controls, Herdr panes and CLI composition.
2. Task 6: versioned binary installer and release automation.
3. Task 7: full integration, usage documentation and acceptance evidence.
4. Parent's final integrated review before any push; publication remains separate.

Keep the requested workflow: fresh `gpt-6-astra` implementer at **low** reasoning,
separate spec and quality gates at **medium**, fixes and scoped re-review, then the
parent's final pass. Local implementation commits are authorized. Do not push,
publish, create credentials, or edit user keybindings as part of this scope.

Task 5 composes `ui.New(resolver, github, cache, now)` with `herdr.Resolver`,
`github.Client`, and `cache.Disk`. Map controls to `SelectSectionMsg` and
`RefreshMsg`; the current `View` is only a placeholder. Add quit cancellation and
render the existing loading, stale, error, warning, timestamp and cooldown state.

[Stack membership and PR navigation](../../followups.md) were explicitly deferred
by the user; do not add them to the remaining initial-release tasks.

## Decisions retained from execution

- Use the supplied checkout on its feature branch. It began without implementation;
  if isolation becomes necessary, moving the work later costs checkout setup.
- Use only owned, isolated Herdr test sessions. This protects the user's panes;
  unavailable isolation may limit live validation. The current controller is outside
  Herdr, and historical caller IDs must not become mutation targets.
- Defer push until concrete final review and authorization. Local commits are
  authorized; this leaves a separate delivery step.
- Exclude Glance and explicitly known plugin pane IDs. Herdr 0.8.2 omits ordinary
  pane ownership; an unknown third-party plugin in a Git directory can still become
  a candidate. Do not infer ownership from titles or process environments.
- Discussion API reads use explicit host/node identity without checkout-dependent
  discovery; Snapshot discovery runs in the working checkout. A host-configuration
  edge case may require explicit per-call metadata later.
- Persist accepted discussion results synchronously in Update, after generation
  validation. This prevents stale or reordered writes without a second scheduler.
  Large caches or slow storage can briefly block the UI; an ordered persistence
  service can be considered if measured latency warrants it.

## Local recovery artifacts

The git-ignored `.superpowers/sdd/2026-09-08-glance-pr/` directory is retained.
It contains the progress ledger, task briefs/reports, both gates and fix reviews,
Linux validation logs, and `integration.json`/`integration-notes.md` with temporary
fixture and tool paths. Do not repeat completed tasks if this directory is lost;
the plan checkboxes, checkpoint and git history record their completion.

GoReleaser 2.18.1 was downloaded to a temporary directory and checksum-verified for
later use; its path is in `integration.json`. The pinned Linux test image digest is
also recorded there. Temporary paths are local conveniences, not runtime dependencies.
